export enum ClothingType {
    MENS = 'M',
    WOMENS = 'W',
    ACCESSORY = 'A'
}

export enum ItemCategory {
    TOP = "T",
    BOTTOM = "B",
    SET = "S",
    FOOTWEAR = "F"
}

export enum Status {
    PACKED = "P",
    OUT = "O",
    RETURNED = "R",
}
